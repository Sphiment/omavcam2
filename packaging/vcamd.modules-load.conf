# Loaded at boot, because a `systemd --user` daemon has no CAP_SYS_MODULE and
# nothing in vcamd ever calls modprobe (ADR-0008). Its parameters are in
# /usr/lib/modprobe.d/vcamd.conf.
#
# An always-present node costs nothing: with exclusive_caps=1 an idle node
# advertises output only, so an idle vcamd is in nobody's camera list.
v4l2loopback
